Setup
Clone or download this repository.

Create a bot using @BotFather on Telegram and get your API Token.

Open the config.py file and paste your bot token:

text
API_TOKEN = "YOUR_BOT_TOKEN"
Run the bot:
text
python main.py
If everything is set correctly, your bot should start running.
